#include "monty.h"

/**
 * f_queue - Sets the format to the data of a queue (FIFO).
 * @head: The head of the stack.
 * @line_number: The line_number.
 * Return: No return.
*/
void f_queue(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	bus.lifi = 1;
}
